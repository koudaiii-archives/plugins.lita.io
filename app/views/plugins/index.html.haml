- content_for :title, "Plugins"

.container
  .page-header
    %h1 Lita Plugins

  %p The following is a list of all plugins available for Lita. Plugins are distributed as gems. To have your plugin added to this list, simply publish it to RubyGems. Consult <a href="http://docs.lita.io/plugin-authoring/publishing/">publishing</a> on the plugin authoring page for details.

  .well
    %p Jump to:
    %a{ href: "#adapters" } Adapters
    = "(#{@plugins['adapter'].size})"
    \-
    %a{ href: "#handlers" } Handlers
    = "(#{@plugins['handler'].size})"
    \-
    %a{ href: "#extensions" } Extensions
    = "(#{@plugins['extension'].size})"
    \-
    %a{ href: "#unknown" } Unknown
    = "(#{@plugins[nil].size})"

  %h2#adapters.spacer= "Adapters (#{@plugins['adapter'].size})"

  .spacer= render @plugins['adapter']

  %h2#handlers.spacer= "Handlers (#{@plugins['handler'].size})"

  .spacer= render @plugins['handler']

  %h2#extensions.spacer= "Extensions (#{@plugins['extension'].size})"

  .spacer= render @plugins['extension']

  %h2#unknown.spacer= "Plugins of unknown type (#{@plugins[nil].size})"

  .spacer= render @plugins[nil]

